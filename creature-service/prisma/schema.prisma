generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Creature {
  id        Int   @id @default(autoincrement())
  name      String
  species   String
  createdAt DateTime @default(now())
}
